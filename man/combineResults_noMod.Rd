% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_margins.R
\name{combineResults_noMod}
\alias{combineResults_noMod}
\title{Combine Results for Marginal Effects and Differences}
\usage{
combineResults_noMod(
  results,
  binary_exposure,
  decimals = 2,
  decimals_pvalue = 4,
  outcome_is_binary = FALSE
)
}
\arguments{
\item{results}{A model object, typically from `marginaleffects`, containing the results of the model.}

\item{binary_exposure}{A string representing the binary exposure variable for the analysis.}

\item{decimals}{An integer specifying the number of decimal places to round the results to (default is 2).}

\item{decimals_pvalue}{An integer specifying the number of decimal places to round p-values to (default is 4).}

\item{outcome_is_binary}{A logical indicating if the outcome variable is binary (default is FALSE).}
}
\value{
A dataframe containing the combined results with optional formatting.
}
\description{
This function combines marginal means, absolute differences, and relative differences
into a single dataframe, allowing for customized formatting and conversions.
}
\examples{
# Example usage:
df <- mtcars
results <- lm(mpg ~ vs, data = df)
combineResults_noMod(results, binary_exposure = "vs")
}
